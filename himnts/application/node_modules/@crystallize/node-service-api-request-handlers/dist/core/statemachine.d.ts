export type StateMachineDefinition = {
    states: {
        [key: string]: {
            onEnter?: (subject: any) => void;
            onEntered?: (subject: any) => void;
            onExit?: (subject: any) => void;
            transitions: {
                [key: string]: {
                    to: keyof StateMachineDefinition['states'];
                    action?: (subject: any) => void;
                };
            };
        };
    };
};
export type StateMachine = {
    subject: any;
    transition: (transitionName: string) => void;
    canTransit: (transitionName: string) => boolean;
};
export declare function createStateMachine(subject: any, stateProperty: string, definition: StateMachineDefinition): StateMachine;
export declare class TransitionError extends Error {
    code: number;
    constructor(message: string);
}
