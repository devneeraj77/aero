import { AddReceiptToOrderArgs, VippsAppCredentials } from './types';
export declare const addVippsReceiptOrder: ({ orderId, paymentType, receipt }: AddReceiptToOrderArgs, credentials: VippsAppCredentials) => Promise<{
    state: 'CREATED' | 'AUTHORIZED' | 'TERMINATED';
}>;
