import { VippsAppCredentials } from './types';
export type ClientInterface = Awaited<ReturnType<typeof createClient>>;
type Credentials = VippsAppCredentials & {
    extraHeaders?: Record<string, string>;
    fetchToken?: boolean;
    oauthConnect?: boolean;
    oauthToken?: string;
};
export declare const createClient: ({ origin, clientId, clientSecret, subscriptionKey, merchantSerialNumber, extraHeaders, fetchToken, oauthConnect, oauthToken, }: Credentials) => Promise<{
    get: <T>(endpoint: string, idempotencyKey: string, init?: RequestInit) => Promise<T>;
    post: <T_1>(endpoint: string, body: any, idempotencyKey: string, init?: RequestInit) => Promise<T_1>;
    put: <T_2>(endpoint: string, body: any, idempotencyKey: string, init?: RequestInit) => Promise<T_2>;
    formEncodedPost: <T_3>(endpoint: string, body: any, idempotencyKey: string, init?: RequestInit) => Promise<T_3>;
}>;
export {};
