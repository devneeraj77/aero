import { VippsAppCredentials } from './types';
export declare const fetchVippsCheckoutSession: (url: string, credentials: VippsAppCredentials) => Promise<{
    session: {
        reference: string;
        sessionId: string;
        sessionState: string;
        paymentDetails?: {
            state: 'CREATED' | 'AUTHORIZED' | 'TERMINATED';
        } | undefined;
    };
    payment: {
        state: "CREATED" | "AUTHORIZED" | "TERMINATED";
    };
} | {
    session: {
        reference: string;
        sessionId: string;
        sessionState: string;
        paymentDetails?: {
            state: 'CREATED' | 'AUTHORIZED' | 'TERMINATED';
        } | undefined;
    };
    payment: null;
}>;
