import { VippsAppCredentials } from './types';
export declare const fetchVippsPayment: (reference: string, credentials: VippsAppCredentials) => Promise<{
    state: 'CREATED' | 'AUTHORIZED' | 'TERMINATED';
}>;
