import { ClientConfiguration } from '@crystallize/js-api-client';
type Action = {
    type: 'LOADING';
    state: boolean;
} | {
    type: 'UPDATE_CONFIGURATION';
    configuration: ClientConfiguration;
} | {
    type: 'CHANGE_LANGUAGE';
    language: string;
};
export type Actions = {
    loading: (state: boolean) => void;
    updateConfiguration: (configuration: ClientConfiguration) => void;
    changeLanguage: (language: string) => void;
};
export type Dispatch = (action: Action) => void;
export type State = {
    configuration: ClientConfiguration;
    loading: boolean;
    language: string;
};
export declare function Reducer(state: State, action: Action): {
    loading: boolean;
    configuration: ClientConfiguration;
    language: string;
};
export declare function mapToReducerActions(dispatch: Dispatch): Actions;
export {};
